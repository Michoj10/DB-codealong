from model import session, Base, Contact

def add_contact(name,phone):
	# creates a new contact and adds it to the list

def get_contacts():
	# returns a list of all the contacts in the database

def delete_contact(con_id):
	# deletes the contact with the id number from the database